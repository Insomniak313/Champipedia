## Champipedia - variables d'environnement
#
# Prisma/DB (prod):
# - PRISMA_DB_URL: URL de connexion utilisée par Prisma (recommandé)
# - POSTGRES_URL: fallback si PRISMA_DB_URL n'est pas défini
#
# Exemple (Postgres):
# PRISMA_DB_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"
# POSTGRES_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"
#
# Dev (optionnel): si aucune variable n'est définie, l'app utilise par défaut:
# file:./prisma/dev.db

